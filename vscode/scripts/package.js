// The build script as it sits on the default branch. Benign.
// It is referenced by name from the workflow (`npm run package`), so its contents come
// from whichever ref the run checked out.
console.log("packaging the extension");
